import { WizardShell } from "../../components/wizard/wizard-shell";

export const metadata = {
  title: "Kinesia — Process video",
};

export default function ProcessPage() {
  return <WizardShell />;
}
